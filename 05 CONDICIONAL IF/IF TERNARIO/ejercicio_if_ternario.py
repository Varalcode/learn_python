# En este ejercicio se verifica si el numero contenido en la variable num es par o impar a través de un if ternario.

num = 200
resultado = f"El número {num} es par" if num % 2 == 0 else f"El número {num} es impar"
print(resultado)
